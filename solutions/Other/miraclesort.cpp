#include <bits/stdc++.h>
#define ll long long
using namespace std;
long long n, b;

int main() {
	ios_base::sync_with_stdio(false); cin.tie(NULL); cout.tie(NULL);
	cin >> n;
	while (n--) {
	    cin >> b;
	    cout << b << '\n';
	}
}